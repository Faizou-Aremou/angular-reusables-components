import { TableColumnHeaderPipe } from './table-column-header.pipe';

describe('TableColumnRefPipe', () => {
  it('create an instance', () => {
    const pipe = new TableColumnHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
