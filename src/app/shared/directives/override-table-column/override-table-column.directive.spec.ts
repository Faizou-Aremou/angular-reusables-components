import { OverrideTableColumnDirective } from './override-table-column.directive';

describe('TableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new OverrideTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
