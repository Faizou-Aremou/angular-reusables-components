import { StaticTabsContentDirective } from './static-tabs-contents.directive';

describe('StaticTabsContentsDirective', () => {
  it('should create an instance', () => {
    const directive = new StaticTabsContentDirective();
    expect(directive).toBeTruthy();
  });
});
