import { StaticTabsContentsDirective } from './static-tabs-contents.directive';

describe('StaticTabsContentsDirective', () => {
  it('should create an instance', () => {
    const directive = new StaticTabsContentsDirective();
    expect(directive).toBeTruthy();
  });
});
