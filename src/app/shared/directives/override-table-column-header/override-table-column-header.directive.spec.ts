import { OverrideTableColumnHeaderDirective } from './override-table-column-header.directive';

describe('TableColumnHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new OverrideTableColumnHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
