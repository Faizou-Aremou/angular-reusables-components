import { LabelFileInputTriggerDirective } from './label-file-input-trigger.directive';

describe('LabelFileInputTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelFileInputTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
